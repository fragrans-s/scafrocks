<p id="notice"><%= notice %></p>

<h1>Janken!</h1>

<table>
  <thead>
    <tr>
      <th>Name a</th>
      <th> </th>
      <th>Name b</th>
      <th> </th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @scafrocks.each do |scafrock| %>
      <tr>
        <td><%= scafrock.name_a %></td>
        <td><%= link_to 'Play A', edit_scafrock_path(scafrock, player: 'a') %></td>
        <td><%= scafrock.name_b %></td>
        <td><%= link_to 'Play B', edit_scafrock_path(scafrock, player: 'b') %></td>
  	  	<td><%= link_to 'Show', scafrock, data: 1 %></td> 
        <td><%= link_to 'Destroy', scafrock, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Game', new_scafrock_path(player: 'a') %>
